apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Argo CD syncs this directory. To bump the running image, change the tag here
# (GitOps) — or let Argo CD Image Updater watch Docker Hub and bump it for you.
images:
  - name: lebovskiis/aegis
    newTag: "0.2.0"

resources:
  - deployment.yaml
  - service.yaml
  - networkpolicy.yaml
